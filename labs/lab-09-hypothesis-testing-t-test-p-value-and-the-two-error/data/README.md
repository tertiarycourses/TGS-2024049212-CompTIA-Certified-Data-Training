# Lab 9 — dataset

An A/B test of a new checkout page.

## Files

- **`abtest.csv`** (1,088 bytes) — 120 rows — 60 in group A (old page), 60 in group B (new page).
- **`abtest.xlsx`** (6,533 bytes) — The same data as an Excel workbook.

---

Data is generated deterministically, so the numbers in the Learner Guide and the slides
always match what you see. Regenerate with:

```bash
python3 .claude/skills/courseware-build/build/make_lab_data.py
```
